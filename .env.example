# Copy this file to .env and fill in your actual values
# .env is gitignored — NEVER push .env to GitHub

DB_HOST=localhost
DB_PORT=3306
DB_NAME=pass_slip_db
DB_USER=root
DB_PASSWORD=your_mysql_password_here
